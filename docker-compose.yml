version: "3.9"

services:
  web:
    build: .
    image: flask-demo:compose
    container_name: flask-web
    ports:
      - "5200:5000"
    environment:
      - FLASK_ENV=production
    restart: unless-stopped
