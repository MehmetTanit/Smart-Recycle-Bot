services:
  builder:
    build: ./app
    volumes:
      - data:/data
    command: ["bash", "-lc", "python main.py && echo 'Fertig – warte 10 Minuten...' && sleep 600"]
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - QDRANT_URL=${QDRANT_URL}

volumes:
  data: